import 'package:flutter/material.dart';

class ItemDictionaryScreen extends StatelessWidget {
  const ItemDictionaryScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('ItemDictionaryScreen'),
      ),
    );
  }
}