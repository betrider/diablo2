import 'package:flutter_diablo2_exchange/index.dart';

class LadderItemRegistrationScreen extends StatelessWidget {
  const LadderItemRegistrationScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.grey,
      ),
    );
  }
}