import 'package:flutter/material.dart';

class FreeSharingScreen extends StatelessWidget {
  const FreeSharingScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('FreeSharingScreen'),
      ),
    );
  }
}