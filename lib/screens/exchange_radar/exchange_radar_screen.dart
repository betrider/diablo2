import 'package:flutter/material.dart';

class ExchangeRadarScreen extends StatelessWidget {
  const ExchangeRadarScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('ExchangeScreen'),
      ),
    );
  }
}