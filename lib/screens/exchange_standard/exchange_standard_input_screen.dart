import 'package:flutter_diablo2_exchange/index.dart';

class ExchangeStandardInputScreen extends StatelessWidget {
  const ExchangeStandardInputScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}